<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_dark"
    android:padding="16dp">

    <!-- Display -->
    <TextView
        android:id="@+id/tvResult"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="0"
        android:textSize="48sp"
        android:textStyle="bold"
        android:gravity="bottom|end"
        android:padding="24dp"
        android:textColor="?android:textColorPrimary"
        android:background="?attr/colorSurface"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/gridLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.35"/>

    <!-- Buttons Grid -->
    <GridLayout
        android:id="@+id/gridLayout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:columnCount="4"
        android:rowCount="5"
        android:useDefaultMargins="true"
        app:layout_constraintTop_toBottomOf="@id/tvResult"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Row 1 -->
        <Button style="@style/CalcButton" android:id="@+id/btnClear" android:text="C"/>
        <Button style="@style/CalcButton" android:id="@+id/btnBackspace" android:text="⌫"/>
        <Button style="@style/CalcButton" android:id="@+id/btnPercent" android:text="%"/>
        <Button style="@style/OperatorButton" android:id="@+id/btnDivide" android:text="÷"/>


        <!-- Row 2 -->
        <Button style="@style/CalcButton" android:id="@+id/btn7" android:text="7"/>
        <Button style="@style/CalcButton" android:id="@+id/btn8" android:text="8"/>
        <Button style="@style/CalcButton" android:id="@+id/btn9" android:text="9"/>
        <Button style="@style/OperatorButton" android:id="@+id/btnMultiply" android:text="×"/>


        <!-- Row 3 -->
        <Button style="@style/CalcButton" android:id="@+id/btn4" android:text="4"/>
        <Button style="@style/CalcButton" android:id="@+id/btn5" android:text="5"/>
        <Button style="@style/CalcButton" android:id="@+id/btn6" android:text="6"/>
        <Button style="@style/OperatorButton" android:id="@+id/btnSubtract" android:text="-"/>


        <!-- Row 4 -->
        <Button style="@style/CalcButton" android:id="@+id/btn1" android:text="1"/>
        <Button style="@style/CalcButton" android:id="@+id/btn2" android:text="2"/>
        <Button style="@style/CalcButton" android:id="@+id/btn3" android:text="3"/>
        <Button style="@style/OperatorButton" android:id="@+id/btnAdd" android:text="+"/>


        <!-- Row 5 -->
        <Button style="@style/CalcButton" android:id="@+id/btn0" android:text="0"/>
        <Button style="@style/CalcButton" android:id="@+id/btnDot" android:text="."/>
        <Button style="@style/CalcButton" android:id="@+id/btnTheme" android:text="🌙"/>
        <Button style="@style/EqualButton" android:id="@+id/btnEqual" android:text="="/>

    </GridLayout>

</androidx.constraintlayout.widget.ConstraintLayout>